{
  "name": "client",
  "version": "1.0.0",
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "serve": "lerna run --parallel serve",
    "build": "lerna run --parallel build"
  },
  "license": "MIT",
  "devDependencies": {
    "lerna": "^3.22.1"
  }
}
